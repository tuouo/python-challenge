#!/usr/bin/env python3
# -*- coding: utf-8 -*-

'''
To be legendary

2: find the latters only appear once in string.
   then twice. (not record times for every latter)

10: reg = re.compile(r'(?P<w>\d)(?P=w)')
    reg = re.compile(r'((?P<w>\d)(?P=w)*)')
    make somebady hnow why

15: getpixel((x, y)) --> 195 means one of pink?

'''